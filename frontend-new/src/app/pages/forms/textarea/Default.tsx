import { Textarea } from "@/components/ui";

const Default = () => {
  return (
    <div className="max-w-xl">
      <Textarea placeholder="متن خود را وارد کنید" rows={5} />
    </div>
  );
};

export { Default };
